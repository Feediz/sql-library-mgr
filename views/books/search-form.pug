div(class="search")
  form(action="/books" method="post")
    input(type="search" name="q" value=`${data.searchTerm}` placeholder="What book are you looking for ...")
    input(type="submit" value="search")
include pagination